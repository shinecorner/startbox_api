description: Save user’s settings
tags:
  - Settings
requestBody:
  $ref: ../../index.yaml#/components/requestBodies/UserSettings
responses:
  200:
    description: OK
    content:
      application/json:
        schema:
            $ref: ../../index.yaml#/components/schemas/User