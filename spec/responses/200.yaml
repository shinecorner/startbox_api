description: OK
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: boolean
          description: If the request behaved as intended
          example: true
        message:
          type: string
          description: The response text
          example: OK