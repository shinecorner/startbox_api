description: Pin login fields
required: true
content:
  application/x-www-form-urlencoded:
    schema:
      type: object
      properties:
        message:
          type: string
          example: 'I see a bug'